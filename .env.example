# Server Configuration
NODE_ENV=development
PORT=4000

# SMTP Configuration (Email)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# AI Configuration
GEMINI_API_KEY=your-gemini-api-key
OLLAMA_MODEL=llava:13b

# Google Cloud Vision API (opcjonalne - dla lepszego OCR)
GOOGLE_VISION_API_KEY=your-google-vision-api-key

# Admin Panel Configuration
ADMIN_PASSWORD=your-secure-admin-password

# Test Authentication (Optional - for restricting access during testing)
TEST_AUTH_ENABLED=false
TEST_AUTH_USER=test
TEST_AUTH_PASS=test-password

# Database (if needed in future)
# DATABASE_URL=postgresql://user:password@host:port/database

# Instructions:
# 1. Skopiuj ten plik jako .env
# 2. Uzupełnij wszystkie wartości
# 3. Dla Gmail SMTP_PASS użyj "App Password" (nie zwykłego hasła)
# 4. Gemini API Key: https://aistudio.google.com/app/apikey
# 5. Google Vision API: https://console.cloud.google.com/
